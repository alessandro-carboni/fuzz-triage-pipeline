# third-party sources (fetched at runtime)
targets/**/src/**

# build outputs (keep directory via .gitkeep)
targets/**/out/*
!targets/**/out/.gitkeep

# fuzz artifacts (keep directories via .gitkeep)
artifacts/corpus/**
artifacts/crashes/**
artifacts/reports/**
!artifacts/**/.gitkeep

# run-based outputs (keep directories via .gitkeep at any depth)
artifacts/runs/**
!artifacts/runs/**/.gitkeep

# triage outputs
artifacts/reports/**
artifacts/repros/**
!artifacts/**/.gitkeep
